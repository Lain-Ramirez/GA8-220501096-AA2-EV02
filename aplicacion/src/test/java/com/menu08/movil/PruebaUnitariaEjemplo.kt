package com.menu08.movil

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Prueba local de ejemplo, la que trae la plantilla: se ejecuta en la maquina de desarrollo.
 * Solo demuestra que el andamiaje de pruebas funciona; las pruebas de verdad llegan con
 * el issue de pruebas unitarias de la aplicacion movil.
 */
class PruebaUnitariaEjemplo {

    @Test
    fun laSumaEsCorrecta() {
        assertEquals(4, 2 + 2)
    }
}
